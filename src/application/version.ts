/**
 * Application Version (Phase 17)
 *
 * Single source of truth for the display version string.
 * Referenced by the version footer — never hardcoded elsewhere.
 *
 * This is an application-layer constant (UI concern only).
 * It does not affect domain logic.
 */

export const APP_VERSION = "1.2";
